info:
  name: LLM - List Models
  type: http
  seq: 3

http:
  method: GET
  url: https://ai.zer0.garden/v1/models
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
