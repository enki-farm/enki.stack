apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudflare

resources:
  - namespace.yaml
  - deployment.yaml

secretGenerator:
  - name: tunnel-token
    type: Opaque
    envs:
      - .env
    options:
      disableNameSuffixHash: true
