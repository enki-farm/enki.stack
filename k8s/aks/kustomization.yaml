apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# AKS-specific lightweight GPU support (opt-in, mirrors --enable-gpu-pool on
# infra/aks/create-aks.sh). DGX Spark uses the NVIDIA GPU Operator instead
# (see infra/k3s/install-gpu-operator.sh).
resources:
  - namespace.yaml
  - gpu-device-plugin.yaml
